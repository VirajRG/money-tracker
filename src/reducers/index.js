const initialState = {
  netAmount : 0
}

const reducers = (state = initialState, action) => {
  switch (action.type) {
    default: 
      return {
        ...state
      }

  }
}
export default reducers